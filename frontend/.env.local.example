# Copy to .env.local — never commit .env.local to git

# Backend Railway URL (local dev: http://localhost:3001)
BACKEND_API_URL=http://localhost:3001

# Must match INTERNAL_API_KEY on Railway backend
INTERNAL_API_KEY=dev-secret-key
