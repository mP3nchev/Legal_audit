# This monorepo's backend service is deployed from the `backend/` subdirectory.
# In Railway dashboard → Service → Settings → Root Directory → set to: backend
# The active railway.toml is at backend/railway.toml
